spring:
  application:
    name: gateway-server-jdk17

  profiles:
    active: dev

  main:
    allow-circular-references: true

  config:
    import:
      - optional:classpath:application-${spring.profiles.active}.yaml
      - optional:nacos:${spring.application.name}-${spring.profiles.active}.yaml